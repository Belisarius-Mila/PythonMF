if znamená „pokud“. Výraz teplota >= 20 ověří, zda je teplota alespoň 20. Je-li podmínka splněna, provede se první odsazený blok; jinak blok po else („jinak“).

Za if i else patří dvojtečka. Odsazení určuje, které řádky patří do které větve. Další porovnání: > větší, < menší, == rovná se. Pozor: = přiřazuje, == porovnává.